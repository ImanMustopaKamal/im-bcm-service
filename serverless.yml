service: im-bcm-service
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x

plugins:
 - serverless-offline

functions:
  api:
    handler: index.handler
    events:
      - httpApi: '*'

custom:
  serverless-offline:
    lambdaPort: 3002
    httpPort: 3000
    host: '0.0.0.0'
